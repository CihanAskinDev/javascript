function getLength(string) {

    const totalLength = string.length;
    return totalLength;
}
const words = "hello";
const stringLength = "The total characters of " + words + "  + getLength" + words.length;


console.log(stringLength);